name: Tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - php: '8.1'
            laravel: '10.*'
          - php: '8.2'
            laravel: '11.*'
          - php: '8.2'
            laravel: '12.*'
          - php: '8.3'
            laravel: '13.*'

    name: PHP ${{ matrix.php }} / Laravel ${{ matrix.laravel }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - name: Install dependencies
        run: >-
          composer update
          --with "illuminate/config:${{ matrix.laravel }}"
          --with "illuminate/database:${{ matrix.laravel }}"
          --with "illuminate/support:${{ matrix.laravel }}"
          --prefer-dist
          --no-interaction
          --no-progress

      - name: Run tests
        run: composer test
